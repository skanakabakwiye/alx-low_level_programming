#include <stdio.h>
#include "main.h"

/**
 * main - check the code.
 *
 * Return: Always 0.
 */

int main(void)
{
    char b;
    int c;
    while(c<1)
    {

    while(b = 'A')
    {
    printf("%b: %d\n", c, _isupper(c));
    }
    while (b = 'a')
    {
    printf("%b: %d\n", c, _isupper(c));
    }
    }
    return (0);
}
