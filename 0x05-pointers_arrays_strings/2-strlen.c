#include "main.h"
/**
 * main - entry point
 * 
 * return: 0
 */

int _strlen(char *s)
{
}

int main ()
{
	char st[5]={'a','b','c','d','\0'};
	_putchar("length is %d", _strlen(char st));
	return (0);

}

